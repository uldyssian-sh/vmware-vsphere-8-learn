name: Weekly Activity

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  activity:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - name: Weekly activity
      run: echo "Weekly activity completed"
