name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Build site
        run: |
          mkdir -p _site
          echo "<!DOCTYPE html>
          <html>
          <head>
              <title>vmware-vsphere-8-learn</title>
              <meta charset='UTF-8'>
          </head>
          <body>
              <h1>vmware-vsphere-8-learn</h1>
              <p>✅ Successfully deployed!</p>
              <p>Repository: <a href='https://github.com/uldyssian-sh/vmware-vsphere-8-learn'>uldyssian-sh/vmware-vsphere-8-learn</a></p>
          </body>
          </html>" > _site/index.html
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
